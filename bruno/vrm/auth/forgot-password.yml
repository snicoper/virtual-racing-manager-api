info:
  name: forgot-password
  type: http
  seq: 8

http:
  method: POST
  url: "{{base-url}}/auth/forgot-password"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
