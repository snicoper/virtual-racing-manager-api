info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{base-url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "salva@example.com",
        "password": "Password123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
